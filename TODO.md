- [ ] Clean up corrupted `usePatientSummary` signature block in `src/hooks/usePatientDetail.ts`
- [ ] Ensure final signature is exactly `export function usePatientSummary(patientId: string | null) {`
- [ ] Remove only stray leftover lines (`) {`, duplicate `return useQuery({`, transitional comments/old fragments)
- [ ] Run `npm run build` to verify Vite/TypeScript compile
- [ ] Report build result, exact cleaned lines, and logic-change confirmation
